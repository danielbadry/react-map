export default function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">React app initialized</p>
        <h1>Ready to build your map experience.</h1>
        <p className="copy">
          This starter uses React with Vite for a fast local workflow.
        </p>
      </section>
    </main>
  );
}
